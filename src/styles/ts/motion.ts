export const motion = {
    transition: {
        fast: "0.1s ease-out",
        base: "0.2s ease-out",
        slow: "0.3s ease-out",
        bounce: "0.3s cubic-bezier(0.16, 1, 0.3, 1)",
    },
} as const;