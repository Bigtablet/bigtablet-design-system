name: Dependency Submission (npm + pnpm)

on:
  push:
    branches: [ main ]
    paths:
      - 'package.json'
      - 'pnpm-lock.yaml'
      - '.github/workflows/dependency-submission-npm-pnpm.yml'
  pull_request:
    branches: [ main ]
    paths:
      - 'package.json'
      - 'pnpm-lock.yaml'
      - '.github/workflows/dependency-submission-npm-pnpm.yml'
  schedule:
    - cron: '0 23 * * *'
  workflow_dispatch:

permissions:
  contents: write
  id-token: write

jobs:
  submit-deps:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v6

      - name: Set up Node.js
        uses: actions/setup-node@v6
        with:
          node-version: '24'

      - name: Generate and submit dependency graph (npm + pnpm)
        uses: advanced-security/component-detection-dependency-submission-action@v0.1.4
        with:
          detectorsFilter: Npm,Pnpm