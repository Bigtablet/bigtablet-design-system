/**
 * 로딩 표시 4종을 고르는 기준 - Storybook meta 설명 전용 문자열.
 *
 * `Skeleton`·`Spinner`·`TopLoading`·`LinearProgress` 네 스토리가 같은 표를 싣는다.
 * 어느 컴포넌트로 들어와도 네 개를 한 번에 비교할 수 있어야 하는데, 문자열을 네 곳에
 * 복사해 두면 컴포넌트가 늘어날 때 네 곳을 다 찾아 고쳐야 한다(#600 리뷰).
 *
 * 런타임 코드가 아니다 - `src/index.ts` 에서 export 되지 않으므로 번들에 들어가지 않는다.
 * 그래서 파일명에 `.docs.ts` 를 붙여 두었다.
 *
 * 백틱을 쓰지 않는 일반 문자열 배열이다. meta 의 template literal 안에서는 인라인 코드를
 * `` \` `` 로 escape 해야 하는데, 여기서는 그 규칙이 필요 없어 표를 그대로 읽을 수 있다.
 */
export const LOADING_COMPARISON = [
	"#### 로딩 표시 4종 중 무엇을 쓰나",
	"",
	"| 상황 | 컴포넌트 |",
	"| --- | --- |",
	"| 목록·카드가 뜨기 전, 들어올 내용의 **모양을 알 때** | `Skeleton` |",
	"| 버튼·카드 안에서 **작게 돌려야 할 때** | `Spinner` |",
	"| 페이지 전환처럼 **화면 전체가 바뀔 때** | `TopLoading` |",
	"| 회원가입 3단계처럼 **끝이 정해진 진행률** | `LinearProgress` |",
	"",
	"기다림이 아니라 결과를 알릴 때는 `Toast`(막지 않음)나 `Alert`(막음)입니다.",
].join("\n");
