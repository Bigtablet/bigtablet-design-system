See [`.claude/skills/`](../.claude/skills/) for the skill definitions.

This directory used to hold a byte-identical copy of that tree. Two copies drift —
nobody remembers to update both — so it is a pointer now, matching the root
[`AGENTS.md`](../AGENTS.md).
